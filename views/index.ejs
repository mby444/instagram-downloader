<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Downloader</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/style.css">
    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon">
</head>
<body>
    <h1 class="text-center text-white p-5">Instagram Downloader</h1>
    <form action="/" method="post" class="container d-flex mt-5 mb-5">
        <input type="url" name="url" placeholder="Instagram URL" class="input form-control" required>
        <input type="submit" value="Download" class="btn-submit btn btn-primary">
    </form>
    <div id="content" class="container-fluid mt-3 mb-3">
        <% if (error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        <% if (ready) { %>
            <button id="download-btn" class="btn btn-success"><a href="/download/<%= movedFile %>" id="download-text" class="text-white text-center" download>Download</a></button>
            <table class="table container mt-5 mb-5 text-muted">
                <thead>
                    <tr>
                        <th scope="row" colspan="2" class="text-center">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Caption</td>
                        <td><%- media.caption.text %></td>
                    </tr>
                    <tr>
                        <td>Taken Date</td>
                        <td><%= media.takenDate %></td>
                    </tr>
                    <tr>
                        <td>Media Type</td>
                        <td><%= media.MediaType %></td>
                    </tr>
                    <tr>
                        <td>Likes</td>
                        <td><%= media.commentCount %></td>
                    </tr>
                    <tr>
                        <td>Comment</td>
                        <td><%= media.likeCount %></td>
                    </tr>
                    <tr>
                        <td>Width &times; Height</td>
                        <td><%= media.sizes.width %> &times; <%= media.sizes.height %></td>
                    </tr>
                </tbody>
            </table>
        <% } %> 
    </div>
    <script src="js/main.js"></script>
</body>
</html>